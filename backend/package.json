{
  "name": "tasksnap-backend",
  "version": "1.0.0",
  "description": "Backend screenshot capture service for TaskSnap",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "npm install && npx playwright install chromium"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "playwright": "^1.60.0"
  }
}
